import 'package:flutter/material.dart';

// Esta clase contiene las constantes de color utilizadas para el tema de la aplicación ARK.
// Ahora son accesibles por todas las pantallas (Kibble, Mapas, Tintes) sin depender de ninguna en específico.
class AppColors {
  // Paleta de colores basada en el estilo de ARK (oscuro/acero)
  static const Color primaryColor = Color(0xFF425C76); // Azul acero (color principal de ARK)
  static const Color secondaryColor = Color(0xFFE5E5E5); // Gris claro (texto y acentos)
  static const Color backgroundColor = Color(0xFF1E1E1E); // Gris oscuro casi negro (fondo principal)
  static const Color accentColor = Color(0xFFC7B18E);

  static const Color cardColor = Color(0xFF2A2A2A);

}